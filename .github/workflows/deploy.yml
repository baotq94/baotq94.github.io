name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: npm

      - run: npm ci

      # `prebuild` regenerates public/posts.json from posts/*.md
      - run: npm run build

      # GitHub Pages serves 404.html for unknown paths; fall back to the client-side app.
      - run: cp dist/baotq94.github.io/browser/index.csr.html dist/baotq94.github.io/browser/404.html

      - uses: actions/upload-pages-artifact@v3
        with:
          path: dist/baotq94.github.io/browser

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
